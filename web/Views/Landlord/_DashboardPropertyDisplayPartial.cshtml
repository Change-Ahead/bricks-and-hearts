@model PropertyViewModel

<div class="border rounded">
    
    <h2 class="font-h2">@(Model.Address.AddressLine1 + (Model.Address.AddressLine2 == null ? " " : ", ") + Model.Address.AddressLine2)</h2>
    <p class="font-smallBody">@Model.Description</p>
    <p class="font-subtitle">@Model.Address.ToShortAddressString()</p>
</div>